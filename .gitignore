# OS metadata
.DS_Store
Thumbs.db

# Editor swap files / settings
*.swp
*.swo
*~
.vscode/
.idea/

# Local environment overrides
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Extend per project stack (Python: __pycache__/, .venv/, *.egg-info/, .pytest_cache/,
# .mypy_cache/, .ruff_cache/; Node: node_modules/, dist/, .next/; Rust: target/;
# Go: vendor/; etc.). Keep this file in sync with what the project actually
# generates so reviewers don't have to scan noise.
